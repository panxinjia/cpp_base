#include <iostream>

using namespace std;

/**
 * 命名空间:
 *  用来 隔离程序实体的名称, 组织和重用代码的编译单元
 * 1. 可以重复声明, 可以嵌套
 * 2. 支持别名 namespace std sl;
 * 3. namespace 中的声明和定义可以分开
 * 
 * 
 * => 声明域结尾要加 ; 定义域不需要加
 */
int main(int argc, char const *argv[])
{

    return 0;
}
